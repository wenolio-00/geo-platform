# Agent Rules

## Codex Subagents

- Use the Codex subagent role name `explorer` for read-only codebase investigation.
- Do not use `explore` as a subagent role name; treat it as a mistaken shorthand for `explorer`.
- Explorer agents should inspect code, report findings, and avoid editing files.
